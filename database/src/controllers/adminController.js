const Admin = require('../models/adminModel');

// Controller methods
const getAllCustomers = async (req, res) => {
    // Retrieve and send customer data to the admin view
};

module.exports = {
    getAllCustomers,
    // ...other methods
};
